import { StyleSheet, View } from 'react-native';
import React from 'react';
import TitleText from '../../theme/Text/TitleText';

const LoadingScreen = () => {
  return (
    <View style={{}}>
      <TitleText text={'Loading Screen'}/>
    </View>
  );
};

export default LoadingScreen;

const styles = StyleSheet.create({});
